- name: nginx install and run
  hosts: frontend
  tasks:
  - name: install nginx server
    ansible.builtin.package:
      name: nginx 
      state: present

- name: start nginx
  ansible.builtin.service:
    name: nginx
    start: started
    enabled: yes
    
